# 开发自检清单 —— 检查结果

> 检查对象：本会话产出的交付物（代码块方案）
> 1. 卡片导入空组修复（importLibraryDB / allCategories）
> 2. 历史快照重启生成修复（snapshotConfig 持久化 + 时序）
> 3. 同类空值注入补充修复（cardsConfig / globalAvailableTags）
> 4. 新增功能：换卡图 + 校验校准（main.js / preload.js / App.vue）
>
> 应用形态：Electron + Vue3 桌面离线应用（无数据库/SQL/MQ/后端框架），相关项按 N/A 处理。
> 说明：本会话「禁止修改源代码」，交付物为待集成的代码块（未实际编译/运行/逐行 diff）。

---

## 一、功能与业务逻辑

- [√] 1. 核心主流程完整 —— 换卡图主流程（选图→转PNG→校验校准→内嵌→回读→落盘）链路完整；三处修复逻辑闭合。
- [√] 2. 异常流程兜底 —— 换卡图含 `try/catch` + 统一 `{success,error}` 返回、用户取消、`sharp` 未安装、卡片数据缺失等兜底。
- [√] 3. 关键边界值 —— 空值(null)、空对象、非字符串、空白串、非 PNG 新图等均已判空/过滤。
- [N/A] 4. 幂等/防重 —— 离线桌面应用无支付/下单类重复请求；换卡图用 `.tmp` + `rename` 原子写，可防半成品脏数据。
- [N/A] 5. 业务状态流转 —— 无多状态流转场景。

## 二、代码质量与可维护性

- [√] 6. 命名见名知意 —— `buildPngChunk / isCharaChunk / embedCardJSONIntoPNG / calibrateCardData / validateCardPNG / replaceCardImage` 等命名清晰。
- [√] 7. 必要注释 —— 关键逻辑均有中文注释，标注了插入位置。
- [×] 8. 上帝类/过长方法 —— 宿主 `App.vue` 超长（约数千行）为既有问题，新增方法会继续叠加；建议后续拆分。
- [×] 9. 硬编码 —— PNG 协议关键字（`chara/IHDR/IEND/tEXt`）、扩展名（`.png/.webp/.json`）等为规范常量但未抽常量；可接受但不符合清单要求。
- [×] 10. 重复代码 —— `calibrateCardData` 与前端 `cardLoader.js` 的 `normalizeCardData` 逻辑重复，且 `embedCardJSONIntoPNG` 与既有 `writeTavernPNGChunk` 部分重叠，未抽取公共方法。

## 三、健壮性与异常处理

- [×] 11. 具体异常子类 —— Node/JS 无强类型异常体系，采用通用 `catch(err)`；符合 JS 惯例，但不满足清单「捕获具体异常」要求。
- [√] 12. 避免吞异常 —— 关键路径异常均返回 error 或写入日志；仅备份/删旧文件等非关键清理处 `catch(){/* 忽略 */}`（有意为之）。
- [√] 13. 非空判断 —— `isPNGBuffer / getCardName / card 判空 / Array.isArray` 等防御到位。
- [N/A] 14. 数据库事务 —— 无数据库。
- [√] 15. 资源关闭 —— 使用 `fs.promises`（自动释放），无可泄漏的 stream/连接资源。

## 四、性能与 SQL

- [N/A] 16~18、20~21 —— 无 SQL/索引/缓存/锁。
- [√] 19. 耗时操作异步化 —— 文件读写与转码均用 `fs.promises` + `await`，主进程不阻塞 UI。

## 五、安全防护

- [N/A] 21. SQL 注入 —— 无 SQL。
- [√] 22. XSS/输入转义 —— 本次改动不渲染用户输入为 HTML；项目已引入 dompurify。
- [N/A] 23. 敏感信息脱敏 —— 本次仅涉及文件路径，无手机号/密码等。
- [N/A] 24. 密钥剥离 —— 本会话未新增密钥；API Key 已有配置持久化机制，无硬编码密钥。
- [√] 25. 越权/权限校验 —— 换卡图 `cardPath` 经 `isPathAllowed` 白名单校验；新图路径来自用户对话框 `addAllowedRoot`，符合既有安全模式。

## 六、日志与监控

- [×] 26. 核心入口/出口 INFO 日志 —— 新增 IPC 无统一业务日志（仅返回 message）。
- [×] 27. 异常完整堆栈 —— `card:replaceImage` 的 catch 仅 `return error.message`，未 `console.error`/写 crash.log 打印完整堆栈。
- [√] 28. 高频日志控制 —— 不在循环体内打印日志。
- [N/A] 29. 健康检查接口 —— 桌面应用无后端健康检查。

## 七、配置与部署

- [N/A] 30. 环境隔离 —— 离线单环境桌面应用。
- [×] 31. 依赖冲突 —— 新增原生依赖 `sharp`，需确认与 `electron-builder` 打包及 Electron ABI 兼容性。
- [N/A] 32. DDL/表结构 —— 无数据库。
- [×] 33. 预启动验证 —— 未实际编译/启动验证（本会话仅提供代码块，未集成运行）。

## 八、自测与交付

- [×] 34. 冒烟测试 —— 未实际运行接口/功能（未集成）。
- [√] 35. 影响旧接口回归 —— 新增 IPC 向后兼容，不影响旧接口；仅「删旧 webp/json」行为需回归验证。
- [×] 36. Diff 逐行复查 —— 未在 Git 上实际改动，未做逐行 diff 复查。

---

## 最终结论

存在 [×] 项（第 8、9、10、11、26、27、31、33、34、36 条），**需返修/确认后再进入集成与上线阶段**。

主要待办：
1. 集成到源码后，实际编译 + 冒烟验证（换卡图各格式中转、重启后快照开关持久化、空值导入）。
2. 确认 `sharp` 与 Electron 打包兼容性。
3. 抽公共命门：`calibrateCardData` 复用 `normalizeCardData`；PNG 关键字抽常量；统一日志与异常堆栈。
4. Git diff 逐行复查，剔除调试残留。

> 说明：本清单为对「代码块方案」的静态自检，N/A 项由桌面应用形态决定。