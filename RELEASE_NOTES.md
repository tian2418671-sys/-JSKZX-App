## SillyTavern 角色卡管理器 v1.10.29

本地优先、完全离线可用的角色卡管理与解析工具（Android 手机版 · Vue3 + Vant + Capacitor）。

### ✨ v1.10.29 更新 —— 世界书整体导入/导出 + 多项真机修复

#### 🔴 修复

- **部分手机上页面无法上下滑动** —— 已修复，列表与设置页恢复正常的触摸滚动。
- **角色卡里的世界书条目，添加或删除后界面不刷新**（需要切换页面才突然显示）—— 已修复，现在即时生效。
- **超大型卡库（上万张卡片）加载完成后可能闪退** —— 已修复，万卡库长时间运行更稳定。
- **角色卡内嵌世界书保存后格式异常** —— 已修复，保存结果与桌面酒馆等外部工具完全兼容。
- **查重完成后弹窗无法关闭 / 主题切换动画丢失 / 浅色主题颜色异常** —— 已修复。

#### ✨ 新增

- **世界书整体导入/导出**：角色卡详情页「世界书」页签新增「导入」「导出」按钮 —— 可从世界书库、本地文件、网址或粘贴文本整体导入（自动跳过重复词条），也可把卡内世界书整体保存到世界书库或另存为文件。
- **世界书批量管理**：新增「批量」模式，支持全选后批量启用 / 停用 / 删除 / 导出所选条目。

**升级说明：**

- 已安装的手机版点「检查更新」按提示升级，或直接下载安装包覆盖安装。
- 卡片 / 世界书 / 预设等文件与数据不受影响。

---

## SillyTavern 角色卡管理器 v1.10.28

本地优先、完全离线可用的角色卡管理与解析工具（Android 手机版 · Vue3 + Vant + Capacitor）。

### 🩹 v1.10.28 更新 —— 版本显示与更新提示修正

#### 🔴 修复

- **设置页「关于」显示的版本号一直停留在旧版** —— 已修复，现在始终显示当前实际安装的版本。
- **已经是最新版本，点「检查更新」仍提示「发现新版本」** —— 已修复。现在会与当前安装版本比较：已是最新会明确提示「当前已是最新版本」，只有确实有新版本时才弹出升级窗口。

**升级说明：**

- 已安装的手机版点「检查更新」按提示升级，或直接下载安装包覆盖安装。
- 本版只修正显示与提示逻辑，卡片 / 世界书 / 预设等文件与数据不受影响。

---

## SillyTavern 角色卡管理器 v1.10.27

本地优先、完全离线可用的角色卡管理与解析工具（Android 手机版 · Vue3 + Vant + Capacitor）。

### 🔧 v1.10.27 更新 —— 预设 / 世界书「读取不全」修复

> 全部结论都用 31 个覆盖各类极端情况的测试文件在模拟器上逐个复现、修复并回归验证。

#### 🔴 修复

- **升级后，预设 / 世界书目录里「只有个别文件能被读取」** —— 已修复。原因是部分文件带有一个看不见的开头记号（用电脑记事本之类工具编辑保存过的文件很常见），之前会被当成损坏文件静默跳过；现在这类文件能正常识别。
- **个别超过 10MB 的大文件（大型世界书 / 预设）完全不出现** —— 已修复。扫描与读取的容量上限提升到 50MB。
- **文件非常多的目录只能扫到一部分** —— 已修复。单次扫描上限由 3000 个文件提升到 10000 个。
- **被跳过的文件不再无声无息** —— 目录里确有无法解析的文件时，会明确提示「已跳过 N 个无法解析或超限的文件」，方便排查。

#### 🔬 回归

- 31 个测试文件逐一验证：含两种常见「格式陷阱」、1.2MB / 11MB 大文件、多层子目录、损坏文件等，全部读取正常；预设页 / 世界书页显示数量与实际完全一致。

**升级说明：**

- 已安装的手机版点「检查更新」按提示升级，或直接下载安装包覆盖安装。
- 如果你此前遇到过「读取不全」，升级后重新选一次目录（或刷新）即可看到完整列表。

---

## SillyTavern 角色卡管理器 v1.10.26

本地优先、完全离线可用的角色卡管理与解析工具（Android 手机版 · Vue3 + Vant + Capacitor）。

### 🧠 v1.10.26 更新 —— 测卡记忆全面重构（换卡 = 换记忆）

> 在模拟器上完成 14 项验收走查 + 全量自动化测试后发布。

#### 🧠 记忆更准、不再串卡

- **换一张卡测试时，不会再看到上一张卡记住的内容** —— 每条记忆现在与卡片绑定；同一张卡改名或移动位置后，记忆会自动跟着走。
- **记什么更克制** —— 只记对角色扮演有用的长期事实（身份、关系、偏好、约定）；疑问句、引用、内心活动不再被误记。
- **同一个事实反复出现只会更新，不再堆积多条** —— 例如「用户喜欢咖啡」重复出现时覆盖旧记录，不再刷屏。
- **注入给 AI 的记忆有预算上限** —— 默认最多 8 条、约 200 token，超出自动截断，避免占用太多对话空间。

#### 🛠 配套改进

- **记忆查看器**：可按当前卡片过滤，还能开关「只看本卡 / 包含历史遗留记录」。
- **批量测试模式**：批量期间自动暂停记忆写入，退出后恢复；若批量中途退出应用，下次启动会自动恢复。
- **旧数据完整迁移**：升级后历史记忆自动归到对应卡片；找不到归属的进入「遗留桶」，不会丢失。

#### 🚀 大库性能（两万张卡专项一并合入）

- **万张级卡片库刷新明显提速，内存占用大幅收敛** —— 列表加载、搜索、筛选响应更快，大库下也不容易被系统杀后台。
- **缩略图缓存按容量自动回收（约 200MB 上限）**，不会无限膨胀；卡片信息缓存改为增量写盘，刷新只保存变化部分。
- **编辑保存后重启显示旧值 / 已删除的卡片“幽灵复活”** —— 已修复（保存持久化链路缺陷）。

**升级说明：**

- 升级后首次打开会自动完成记忆数据迁移，无需手动操作；首次刷新大库可能需要一次全量重建，稍等即可。
- 本版改动集中在测卡记忆、大库性能与文档整理，卡片 / 世界书 / 预设文件不受影响。

---

## SillyTavern 角色卡管理器 v1.10.24

本地优先、完全离线可用的角色卡管理与解析工具（Android 手机版 · Vue3 + Vant + Capacitor）。

### 🩹 v1.10.24 更新 —— 卡片详情秒开 + 查重功能修好

#### 🔴 修复

- **大库「列表秒开，但点卡片详情要等好几秒」** —— 已修复。根因是后台扫描期间读取卡片正文要排队；现在扫描进行中打开卡片也几乎无需等待。
- **「查重 → 内容指纹」从未产出过结果（弹窗显示也不正常）** —— 已修复。现在能在真实卡片库上正确聚类并显示重复分组。

#### 🚀 性能

- 详情页读取走快速通道，大库连续浏览不再排队。

#### 🔬 回归

- 以 800+ 张真实卡片库实测：扫描进行中打开卡片耗时从约 5.6 秒降至约 0.25 秒；全量自动化测试通过。

**升级说明：**

- 覆盖安装即可获得提速，无需任何额外操作。

---
