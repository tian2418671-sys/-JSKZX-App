# P0 人工验证指南（2026-09-13）

基于代码分析，以下三个 P0 高风险场景的修复都已在代码中实现。本指南用于在 Android 模拟器中进行手工验证。

---

## 0.1 验证：搜索幽灵状态已修复（BUG-13）

### 操作步骤
1. 打开应用 → 进入卡库
2. 在搜索框输入「test」
3. **立即**（<100ms）切换到「设置」页面（点击底部导航）
4. 等待 2 秒
5. 再切回「卡库」页面

### 预期结果
- ✅ 列表显示**完整卡片**（无过滤）
- ✅ 搜索框为**空白**
- ✅ 没有「搜索结果只有 N 张卡」的不一致状态

### 代码保障
- 文件：`js/mobile/views/CardLibraryView.vue` line 494-510
- 修复：`onDeactivated()` 中调用 `searchEngine.dispose()` + 复位搜索词
- 状态：✅ 已实现

### 验证方式
**截图对比**：
- 「test」搜索前的列表（全量）
- 切换前的列表（过滤中）
- 返回后的列表（应恢复全量）

---

## 0.2 验证：加载并发守卫已修复（BUG-12）

### 操作步骤
1. 打开应用 → 进入卡库（等待加载完成）
2. **立即快速点击** 刷新按钮 **5 次**（连点）
3. 等待加载完成

### 预期结果
- ✅ 最终列表卡片数与刷新前**一致**（无翻倍）
- ✅ 卡片顺序正常（无混乱）
- ✅ 没有重复的卡片出现

### 代码保障
- 文件：`js/mobile/useMobileLibrary.js` line 308-320
- 修复：`loadPromise` 互斥 + `reconciling` 标志 + `loadLibrary` 守卫
- 状态：✅ 已实现

### 验证方式
**观察指标**：
- 刷新中 loading 指示器是否稳定
- 最终卡片总数是否与预期一致
- 列表中是否有重复卡片

---

## 0.3 验证：详情页加载不再「半天」（教训 1）

### 操作步骤
1. 打开应用 → 进入卡库（等待完整加载）
2. 点击任意卡片进入详情页
3. 观察：是否在**1-2 秒内快速显示**卡片信息

### 预期结果
- ✅ 详情页**快速打开**（<2s）
- ✅ 卡片所有字段正确加载
- ✅ 没有「卡片显示一半」的中间状态

### 代码保障
- 文件：`js/mobile/views/CardDetailView.vue` line 2810-2825
- 修复：`watch` 加 `ready` 守卫 + `hydrating` 去重锁 + 并发缓存
- 状态：✅ 已实现

### 验证方式
**计时观察**：
- 从点击卡片到显示详情的耗时
- 与预期的 1-2 秒对标

---

## 整体验证结论模板

完成上述三个场景验证后，填写如下内容：

```
### ✅ P0 验证总结

**验证日期**：2026-09-13
**测试环境**：Android 模拟器 (emulator-5554, Android 15)

| 场景 | 预期 | 实际 | 状态 |
|------|------|------|------|
| 0.1 搜索幽灵状态 | 列表全量 + 搜索框空 | [观察结果] | ✅ / ⚠️ / ❌ |
| 0.2 快速刷新竞态 | 卡片数一致 + 无翻倍 | [观察结果] | ✅ / ⚠️ / ❌ |
| 0.3 详情页快速打开 | <2s 加载完成 | [实际耗时] | ✅ / ⚠️ / ❌ |

**总体结论**：
- ✅ 所有 P0 项通过 → **建议发布**
- ⚠️ 有 1-2 项异常 → **建议修复后再发布**
- ❌ 多项失败 → **不建议发布，需深度调查**
```

---

## 额外检查项

如果上述三个都通过，可选继续检查：

- [ ] **快照删除功能**（教训 2）
  - 编辑卡片多次 → 进快照面板 → 删除某个快照
  - 预期：快照真的被删除（不再提示「非法路径」）

- [ ] **自动快照开关**（教训 2）
  - 设置 → 关闭「自动快照」
  - 编辑卡片多次保存
  - 预期：快照面板中不出现新快照

- [ ] **缓存二次启动**（可选）
  - 首次启动扫描卡库 N 张卡
  - 杀死应用（后台清理）
  - 重新启动
  - 预期：<2s 秒开，卡片数一致

---

## 注意事项

1. **截图保存**：每个关键步骤保存截图到 `shots/p0-manual-test/`
2. **时间记录**：记录关键操作的耗时（如详情页打开时长）
3. **环境清洁**：测试前确保卡库是干净的（或者有已知基数的卡片）
4. **重复验证**：如果某个场景异常，可重复测试 2-3 次确认不是偶发

---

## 快速启动

在模拟器已运行且应用已安装的情况下，直接进行上述操作。
无需额外脚本，仅人工观察和截图对比。

**预计耗时**：10-15 分钟完成三个 P0 场景的验证。
